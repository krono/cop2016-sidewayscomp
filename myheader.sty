\usepackage[pass,letterpaper]{geometry}

\usepackage[utf8]{inputenx}
\input{ix-utf8enc.dfu}
\usepackage[T1]{fontenc}
\usepackage{textcomp}
\usepackage{%
  etoolbox,
  amssymb,
  mathtools,
  txfonts,
  babel,
  relsize,
  csquotes,
  xspace,
  balance}
\usepackage{%
  booktabs,
  multirow,
  rotating,
  ifdraft,
  color,
  chngcntr,
}
\usepackage[table,svgnames,usenames,dvipsnames,x11names]{xcolor}

\usepackage[shortcuts]{extdash}
\usepackage[activate,final]{microtype}[2011/08/18]
\microtypesetup{protrusion,stretch=9,shrink=15,step=3,letterspace=25,tracking=smallcaps}

% macros for comments
\usepackage[%
  obeyDraft,%
  colorinlistoftodos,%
]{todonotes}

\newcommand\todoauthor[2][\empty]{
  \expandafter\newcommand\csname #2\endcsname[2][\empty]{%
    \todo[color=#1,##1]{#2: ##2}}}

\def\todosec{\par\noindent\todo[inline]}
\def\secmissing{\par\noindent\todo[color=red,inline,size=\Large]}
\long\def\todolist#1#2{%
\par\noindent%
\todo[inline,color={red!100!green!50},caption={#1}]{%
\begin{minipage}{\linewidth}%
\begin{itemize}
#2%
\end{itemize}
\end{minipage}
}%
}
\ifdraft{%
  \usepackage{blindtext}
  \AtEndDocument{\listoftodos}
}{
  \let\blindtext\relax
  \let\Blindtext\relax
  \let\blinddocument\relax
  \let\Blinddocument\relax
}


\usepackage[final]{listings}
% Code listings
\lstset{%
  basicstyle=\small\ttfamily,
  % keywordstyle=\footnotesize\ttfamily,
  % commentstyle=\footnotesize\ttfamily,
  columns=fullflexible,
  captionpos=t,
  floatplacement=htbp,
  escapechar=§,
  showstringspaces=false,
  numbers=left, numberstyle=\tiny\color{black!33},numbersep=5pt}
% \AtBeginDocument{
%   \lstMakeShortInline[language=Python]!
%   \lstMakeShortInline[language=Scheme]|
% }

% hack
\let\bibfont\relax
\usepackage[backend=biber,natbib=true,style=numeric]{biblatex}
\setcounter{biburllcpenalty}{7000}
\setcounter{biburlucpenalty}{8000}
\ExecuteBibliographyOptions{
  giveninits=true,
  % doi=true,
  doi=false,
  url=false,%
  isbn=false,
  maxnames=20,%
}
\renewcommand*\bibsetup{\softraggedright}

\usepackage[hyperfirst=false,acronym,shortcuts]{glossaries}
\glsunsetall[main]
\GlsDeclareNoHyperList{acronym}
\setacronymstyle{long-sc-short}
\renewcommand\acronymfont[1]{{\larger\textsc{#1}}}%
\renewcommand*\acrpluralsuffix{{\smaller\glsupacrpluralsuffix}}


\usepackage[binary-units,detect-mode=true]{siunitx}
\sisetup{detect-all,separate-uncertainty=true}

\usepackage[%
  final,plainpages=false,unicode,pdfencoding=auto,
  % pdfpagelabels=false,
  % hidelinks, %
]{hyperref}

 \urlstyle{sf}
\nocaptionrule
\frenchspacing
\raggedbottom
% \setkeys{Gin}{draft=false} % Show images in draft mode
% \graphicspath{ {graphics/} }
\AtBeginDocument{\def\subsectionautorefname{section}}


%%% Local Variables:
%%% mode: latex
%%% TeX-master: "cop2016-sidewayscomp"
%%% End:
